<section>
    <% results.forEach( book => { %>
      <div>
        <img src="<%= book.image_url %>" alt="">
        <h1><%= book.title%></h1>
        <h3><%= book.authors%></h3>
        <p><%= book.description %></p>
      </div>
    <% }) %>
  </section>